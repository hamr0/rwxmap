{
  "provenance": "E25 pass 8. The brief's hand read-verb list, applied to POST/PATCH lowering only, on top of the shipped HAND_READ_VERBS and verbs.json `read` list. `validate` is held out in the default list and scored separately, because E12 measured it as the source of the validateCode wrong loosening (a successful check consumes the OTP).",
  "verbs": ["ask", "asks", "generate", "generates", "verify", "verifies", "compute", "computes", "calculate", "calculates", "estimate", "estimates", "preview", "previews", "simulate", "simulates", "render", "renders", "convert", "converts", "translate", "translates", "match", "matches", "matching", "discover", "discovers"],
  "withValidate": ["validate", "validates"]
}
