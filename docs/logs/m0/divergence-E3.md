# M0 divergence report

## Counts

- rows joined: 292
- unjoined (in run, not in ground truth): 0
- unjoined (in ground truth, not in run): 0
- agree: 222
- WRONG-LOOSENING: 60
- over-tight: 10

By method:
  - POST: {"agree":81,"WRONG-LOOSENING":53,"over-tight":4}
  - GET: {"agree":96}
  - PUT: {"agree":7,"WRONG-LOOSENING":1}
  - DELETE: {"agree":35,"WRONG-LOOSENING":6}
  - PATCH: {"over-tight":6,"agree":3}

By confidence:
  - low: {"agree":62,"WRONG-LOOSENING":53,"over-tight":6}
  - high: {"agree":144,"WRONG-LOOSENING":7}
  - method-only: {"agree":16,"over-tight":4}

## Wrong loosenings

| repo | method | path | operationId | rule class | confidence | rule_id | evidence | gt_class | gt reason |
|---|---|---|---|---|---|---|---|---|---|
| ApplicationEndpointRegistration | POST | /application-endpoint-lists | registerApplicationEndpoints | w | low | A4-loosen | method=POST verb=register | x | ApplicationEndpointListId is described as "generated by the system upon successful registration", so each call to register endpoints mints a brand-new list identifier rather than replacing an existing one. |
| ApplicationProfiles | POST | /application-profiles | createApplicationProfile | w | low | A4-loosen | method=POST verb=create | x | The 201 Created response returns a new ApplicationProfile object with a fresh applicationProfileId each time the operation succeeds, so repeating the call produces another profile rather than the same one. |
| BlockchainPublicAddress | POST | /nonce | createBlockchainPublicAddressValidationNonce | w | low | A4-loosen | method=POST verb=create | x | The 201 Created response returns a freshly generated "One-time challenge" nonce each call, so calling twice yields two distinct nonces rather than the same result. |
| CapabilitiesAndRuntimeRestrictions | POST | /retrieve | postServiceCapability | r | low | A4-loosen | method=POST verb=retrieve | x | The info.description explains the server "internally registers for capability change events if 'subscriptionRequest' is provided in the request" and calls the client-supplied sink with CloudEvents, i.e. it can create a live notification subscription as part of the call. |
| CarrierBillingCheckOut | POST | /payments/{paymentId}/refunds | createRefund | w | low | A4-loosen | method=POST verb=create | x | Description says it will "Create a new refund for an existing payment" which is "total`` or `partial`", moving money back to the user on every successful call, a new refund resource each time. |
| CarrierBillingCheckOut | POST | /payments | createPayment | w | low | A4-loosen | method=POST verb=create | x | Description states "Create a new payment for subsequent charging to an end user. Carrier Billing Server will apply the charging according to business configuration", so each call creates a new payment and reaches the user's bill/balance. |
| CarrierBillingCheckOut | POST | /payments/{paymentId}/validate | validatePayment | r | low | A4-loosen | method=POST verb=validate | x | The 409 response example states "Payment already validated", showing that repeating the same validate call after success does not have the same effect as the first call. |
| ClickToDial | POST | /calls | createCall | w | low | A4-loosen | method=POST verb=create | x | Description says it "Creates a new Click to Dial call session between the caller and the callee", triggering a live network call that rings a device someone else answers, and creates a new call session on every invocation. |
| ConnectedNetworkType | POST | /subscriptions | createConnectedNetworkTypeSubscription | w | low | A4-loosen | method=POST verb=create | x | Description says it will "Create a subscription for receiving notifications on changes to the connected network type of a device", and the callback section confirms the server will POST CloudEvents to the client-provided sink, i.e. a new subscription and notification channel on every call. |
| ConnectivityInsights | POST | /subscriptions | createSubscription | w | low | A4-loosen | method=POST verb=create | x | Summary is "Create a Connectivity insights subscription for a device", and the callbacks section defines a notification POST to the client-supplied sink URL, so each call creates a new subscription/notification channel. |
| ConsentManagement | POST | /consents | createConsent | w | low | A4-loosen | method=POST verb=create | x | Description says the API Provider "will set the creation date of the User Consent to the current date and time when the createConsent operation is invoked", and the response returns a new consentId, so each successful call creates another Consent resource. |
| DedicatedNetworks | POST | /accesses | createAccess | w | low | A4-loosen | method=POST verb=create | x | createAccess grants devices permission to use the dedicated network's reserved connectivity resources and returns a new accessId each call, per "the API returns an accessId... which remains unchanged during its lifetime". |
| DedicatedNetworks | DELETE | /accesses/{accessId} | deleteAccess | w | high | A1-agree | method=DELETE verb=delete | x | Per "Deletion of the access can be interpreted as removal of the permission for all the devices included in the access to access the network," deleting revokes live network connectivity granted to those devices. |
| DedicatedNetworks | POST | /accesses/{accessId}/devices/remove | removeDevicesFromAccess | w | low | A4-loosen | method=POST verb=remove | x | Per "devices can be removed from it," the API returns confirmation that removal is "interpreted as removal of the permission," revoking those devices' live access to the dedicated network. |
| DedicatedNetworks | POST | /networks | createNetwork | w | low | A4-loosen | method=POST verb=create | x | createNetwork reserves connectivity resources and returns a new networkId each call, since "the API returns a networkId... which remains unchanged during its lifetime," and the created network can later be activated for device use. |
| DedicatedNetworks | DELETE | /networks/{networkId} | deleteNetwork | w | high | A1-agree | method=DELETE verb=delete | x | Per "Deletion of a network can be interpreted as a cancellation of the resource reservation," and since an ACTIVATED network is one "devices with access can use," deleting it terminates connectivity devices may currently be using. |
| DeviceDataVolume | POST | /subscriptions | createDeviceDataVolumeSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating a device data volume subscription registers a new webhook subscription for threshold events, matching "API consumers can receive notifications regarding data volume usage thresholds" via a newly created subscription id. |
| DeviceLocation | POST | /subscriptions | createGeofencingSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating a geofencing subscription registers device enter/exit monitoring and returns "an Event Subscription ID," a newly created notification resource each call. |
| DeviceReachabilityStatus | POST | /subscriptions | createDeviceReachabilityStatusSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating this subscription registers a new event subscription so the "API consumer is able to be notified whether the reachability status... has changed," a newly created notification resource each call. |
| DeviceRoamingStatus | POST | /subscriptions | createDeviceRoamingStatusSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating this subscription lets the "API consumer... be notified whether the roaming status of a certain user device has changed," registering a new notification resource each call. |
| DeviceStatus | POST | /subscriptions | createConnectedNetworkTypeSubscription | w | low | A4-loosen | method=POST verb=create | x | Creates a new Subscription resource (id assigned, 201 Created) that triggers ongoing notification delivery to the consumer's sink whenever the connected network type changes. |
| DeviceStatus | POST | /subscriptions | createDeviceReachabilityStatusSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating this subscription (201, with a notifications callback) initiates ongoing reachability-change event delivery to the consumer's sink. |
| DeviceStatus | POST | /subscriptions | createDeviceRoamingStatusSubscription | w | low | A4-loosen | method=POST verb=create | x | Creating a roaming-status subscription (201, with roaming-on/off/country-change events) starts a stream of notifications to the consumer's sink. |
| EdgeApplicationManagement | POST | /app-instances | createAppInstance | w | low | A4-loosen | method=POST verb=create | x | description 'Ask the Edge Cloud Platform to instantiate an application to an Edge Cloud Zone' creates a new running instance with a fresh appInstanceId on each accepted call. |
| EdgeApplicationManagement | POST | /deployments | createAppDeployment | w | low | A4-loosen | method=POST verb=create | x | description 'Ask the Edge Cloud Platform to instantiate an application to several Edge Cloud Zones' creates a new appDeploymentId resource on each accepted call. |
| InHomeDeviceManagement | DELETE | /v1/devices/{deviceId} | deleteDevice | w | high | A1-agree | method=DELETE verb=delete | x | description notes deletion also removes 'Any active access control rules for this device,' restoring the device's live network access, the same class of device-reaching action as updateDevice's block/unblock. |
| IoTDeviceManagement | POST | /enable | enableEsimProfile | w | low | A4-loosen | method=POST verb=enable | x | description 'enabling automatically disables the currently active eSIM Profile' is an over-the-air action that changes the device's live cellular connectivity. |
| IoTDeviceManagement | POST | /disable | disableEsimProfile | w | low | A4-loosen | method=POST verb=disable | x | description 'Device will lose cellular connectivity until another eSIM Profile is enabled' shows this over-the-air call directly changes the device's live connectivity. |
| IoTDeviceManagement | POST | /delete | deleteEsimProfile | w | low | A4-loosen | method=POST verb=delete | x | description 'Permanently removes an eSIM Profile from the device. This operation is irreversible' is an over-the-air action executed on the physical eUICC. |
| IoTDeviceManagement | POST | /set-fallback | setFallbackEsimProfile | w | low | A4-loosen | method=POST verb=set | x | though the operation 'sets attribute only' with 'No state change,' the info.description's over-the-air note lists set-fallback among the operations delivered directly to the device. |
| IoTNetworkOptimization | POST | /features/power-saving | activatePowerSaving | w | low | A4-loosen | method=POST verb=activate | x | summary 'Activate or de-activate power-saving features for IoT devices' adjusts live device network parameters, and the 409 example 'Device already in the requested power-saving state or a conflicting operation is in progress' shows repeating the call does not have the same effect. |
| IoTSIMFraudPrevention | POST | /subscriptions | subscribeFraudPrevention | w | low | A4-loosen | method=POST verb=subscribe | x | Each call to POST /subscriptions creates a new Subscription resource with a generated id and registers a sink that receives future notifications, so repeating it is not equivalent to doing it once. |
| ModelAsAService | POST | /knowledge-bases | createKnowledgeBase | w | low | A4-loosen | method=POST verb=create | x | createKnowledgeBase generates a new unique knowledgeBaseId on every successful call ('Returns full KnowledgeBase object with generated ID'), so calling it twice produces two knowledge bases rather than one. |
| ModelAsAService | POST | /knowledge-bases/{knowledgeBaseId}/tools | createTool | w | low | A4-loosen | method=POST verb=create | x | createTool 'Returns full Tool object with generated toolId' on 201, so each call mints a new tool resource rather than converging on one state. |
| ModelAsAService | POST | /assistants | createAssistant | w | low | A4-loosen | method=POST verb=create | x | createAssistant returns 201 with 'the created Assistant resource (with generated assistantId)', so each call produces a new distinct assistant. |
| ModelAsAService | POST | /answer | queryAssistant | r | low | A4-loosen | method=POST verb=query | x | queryAssistant's LLM 'MAY invoke [associated] tools automatically while generating the answer ... turning the assistant into an agent that can take actions', and those tool invocations reach consumer-registered third-party urls, so the read-shaped query can carry third-party side effects the response never surfaces. |
| MultiPointVPN | POST | /networks | createNetwork | w | low | A4-loosen | method=POST verb=create | x | createNetwork returns 201 with a Location header for 'the newly created network resource', and info.description notes the physical CE-to-cloud-PE link 'is default deployed for one click calling', so each call provisions a new distinct VPN network. |
| NetworkAccessManagement | POST | /reboot-requests | createRebootRequest | w | low | A4-loosen | method=POST verb=create | x | Each call creates a new Reboot Request resource that triggers an immediate or scheduled physical reboot of operator equipment, a live device action on hardware the caller does not directly control. |
| NetworkAccessManagement | POST | /trust-domains/{trustDomainId}/devices | createTrustDomainDevice | w | low | A4-loosen | method=POST verb=create | x | Registration can carry `bootstrappingInfo` that is "used to onboard ('zero-touch') the device", an action taken against the physical device itself rather than a pure data record, even though duplicate registration is rejected with 409 ALREADY_EXISTS. |
| NetworkSliceBooking | POST | /slices/{sliceId}/devices | assignDevice | w | low | A4-loosen | method=POST verb=assign | x | Assigning a device provisions it onto the live network slice and is capacity-bounded ("must not exceed the maximum number defined in the slice QoS profile"), i.e. a per-device reservation against finite slice capacity, not a plain idempotent record update. |
| NetworkSliceBooking | POST | /slices | createSlice | w | low | A4-loosen | method=POST verb=create | x | The Introduction states this API lets consumers "reserve a slice resource of a selected area within a period", a network-resource reservation/booking with each call producing a distinct sliceId. |
| OTPValidation | POST | /validate-code | validateCode | r | low | A4-loosen | method=POST verb=validate | w | Verifies the code for the given authenticationId; once validated the code is consumed (single-use, per MAX_OTP_CODES/expiry semantics) so the authenticationId's final state converges after one or more calls, with no effect reaching outside the API. |
| QoSBooking | POST | /qos-bookings | createBooking | w | low | A4-loosen | method=POST verb=create | x | Description says the operation "reserves a Quality of Service (QoS) booking for a specified number of devices, service area, and scheduled time/duration", a network-resource reservation producing a new bookingId each call. |
| QoSBooking | POST | /qos-bookings/{bookingId}/devices/assign | assignDevices | w | low | A4-loosen | method=POST verb=assign | x | Assignment causes "the network [to be] available for the said devices to connect and use them at the requested time", provisioning live QoS onto specific devices and consuming the booking's finite `remainingDevices` count per device assigned. |
| QoSBooking | POST | /device-qos-bookings | createBooking | w | low | A4-loosen | method=POST verb=create | x | "Triggers a new booking to assign certain QoS Profile to certain device", reserving network QoS capacity and scheduling live provisioning for that device, a distinct booking created per call. |
| QualityOnDemand | POST | /qos-assignments | createQosAssignment | w | low | A4-loosen | method=POST verb=create | x | Creating an assignment "configures the network to apply the requested QoS profile to a specified device whenever the device is connected," and a second call for the same device returns 409 CONFLICT rather than repeating the same effect. |
| QualityOnDemand | POST | /sessions | createSession | w | low | A4-loosen | method=POST verb=create | x | createSession configures the network to provide prioritized latency/throughput for real traffic and a repeat request for the same device/flow returns 409 CONFLICT, so it does not have the same effect each time. |
| QualityOnDemand | POST | /sessions/{sessionId}/extend | extendQosSessionDuration | w | low | A4-loosen | method=POST verb=extend | x | Extending duration adds requestedAdditionalDuration to the existing session length each time it is called, so repeating the same extend request further lengthens a live prioritized network flow rather than reproducing one fixed state. |
| SessionInsights | POST | /sessions | createSession | w | low | A4-loosen | method=POST verb=create | x | Session creation "implicitly establishes a notification subscription" and produces a new operator-assigned session resource on every call. |
| SimSwap | POST | /subscriptions | createSimSwapSubscription | w | low | A4-loosen | method=POST verb=create | x | This creates a new sim-swap event subscription resource, and the API-functionality text confirms the endpoint "allows to ... create new event subscriptions." |
| SponsoredData | DELETE | /sponsorship/{sponsorId}/{campaignId}/{sessionId}/revoke | revokeSponsorship | w | high | A1-agree | method=DELETE verb=revoke | x | revokeSponsorship "revokes an active sponsorship, preventing further use of sponsored data for the subscriber," cutting off a real end user's active benefit rather than the sponsor caller's own resource. |
| SponsoredData | POST | /campaign/{sponsorId}/{campaignId}/alert-subscription | configureAlerts | w | low | A4-loosen | method=POST verb=configure | x | configureAlerts registers a webhook and callbackToken so the sponsor "subscribe[s] to receive notifications related to campaigns," matching the subscription-creation criterion for consequential actions. |
| TrafficInfluence | DELETE | /traffic-influences/{trafficInfluenceID} | deleteTrafficInfluence | w | high | A1-agree | method=DELETE verb=delete | x | deleteTrafficInfluence is "invoked ... to stop influencing the traffic," reversing an active network routing configuration for the device's live traffic rather than merely discarding inert data. |
| VerifiedCaller | POST | /registrations | createRegistration | w | low | A4-loosen | method=POST verb=create | x | createRegistration "creates a new registration and returns a registration identifier," establishing branding that will be displayed to real callees on future calls. |
| VerifiedCaller | POST | /pre-announce | createPreAnnouncement | w | low | A4-loosen | method=POST verb=create | x | createPreAnnouncement can trigger "a short message ... sent to the callee to verify authenticity of the caller" (SMS strategy) or a brand display shown to the called party, a consequential action reaching a third party on each call. |
| WebRTC | POST | /sessions | createSession | w | low | A4-loosen | method=POST verb=create | x | The use-case text says creating a session "grants ability to reach any phone number on the public network using your telco provider rights," and each successful call returns a fresh mediaSessionId, so it both reaches a third party and creates a new resource each time. |
| WebRTC | DELETE | /sessions/{mediaSessionId} | deleteSessionById | w | high | A1-agree | method=DELETE verb=delete | x | The description states this DELETE will "Terminate a 1-1 an ongoing media session" and "trigger all necessary action on the network side to properly hangup and drop the call," ending a live call the other party is on. |
| WebRTC | PUT | /sessions/{mediaSessionId}/status | updateSessionStatus | w | high | A1-agree | method=PUT verb=update | x | The walkthrough for picking up a call uses this PUT to send the SDP "answer" and "include the information about the responding device," so it drives live IMS call signaling that reaches the other party. |
| WebRTC | POST | /subscriptions | createNotificationChannelSubscription | w | low | A4-loosen | method=POST verb=create | x | Each POST creates a new subscription resource and registers a callback "sink" that the server calls for future events, matching both the new-resource-per-call and notification-channel criteria for x. |
| WebRTC | POST | /sessions | createRegistration | w | low | A4-loosen | method=POST verb=create | x | Each successful call causes the server to return "a server-allocated registrationId," and a duplicate (deviceId, phoneNumber) pair is rejected with 409 rather than reused, so repeating the call does not have the same effect as doing it once. |

## Over-tightenings

| repo | method | path | operationId | rule class | confidence | rule_id | evidence | gt_class | gt reason |
|---|---|---|---|---|---|---|---|---|---|
| ApplicationProfiles | PATCH | /application-profiles/{applicationProfileId} | updateApplicationProfile | x | low | A3-keep-method | method=PATCH verb=update | w | Description states it updates "the complete set of network quality thresholds for an application with the new set of thresholds", i.e. a full replace of the identified profile's thresholds. |
| ConsentManagement | PATCH | /consents/{consentId} | updateConsent | x | low | A3-keep-method | method=PATCH verb=update | w | Description says it allows the consumer "to update the status of an existing Consent" identified by consentId, setting consentStatus to GRANTED or DENIED, a set operation on an existing resource. |
| EdgeApplicationManagement | PATCH | /deployments/{appDeploymentId} | updateAppDeployment | x | low | A3-keep-method | method=PATCH verb=update | w | description specifies JSON Merge Patch semantics where 'Only the fields provided in the request body will be updated,' a repeatable set-style update. |
| KnowYourCustomer | POST | /fill-in | KYC_Fill-in | x | method-only | M1 | method=POST | r | The summary describes 'Providing information related to a customer identity stored the account data bound to the customer's phone number', a read of Operator-held data with no write. |
| KnowYourCustomerFill-in | POST | /fill-in | kycFillin | x | method-only | M1 | method=POST | r | As the standalone Fill-in repo's endpoint, it lets a client 'request and receive the information for a particular user ... on file' with the Operator, purely retrieving stored attributes. |
| MultiPointVPN | PATCH | /networks/{serviceId} | updateNetwork | x | low | A3-keep-method | method=PATCH verb=update | w | updateNetwork uses application/merge-patch+json against NetworkUpdate, a JSON-merge-patch set of guaranteeBandwidth/sla fields that is idempotent by construction. |
| MultiPointVPN | POST | /assessment | assessConnectionFeasibility | x | method-only | M1 | method=POST | r | The request body only carries hypothetical connection parameters and the response returns waitDays/sla/rent estimates with no network or resource created, matching the query-only-POST rule for r. |
| NetworkAccessManagement | PATCH | /trust-domains/{trustDomainId} | updateTrustDomain | x | low | A3-keep-method | method=PATCH verb=update | w | Updates the Trust Domain by ID with full-replacement semantics for accessDetails, so repeating the same PATCH body leaves the domain in the same state. |
| NetworkAccessManagement | PATCH | /trust-domains/{trustDomainId}/devices/{deviceId} | updateTrustDomainDevice | x | low | A3-keep-method | method=PATCH verb=update | w | Only included fields are updated, with `bootstrappingInfo`/`deviceCredential` replaced wholesale when present, making repeated identical PATCHes converge on the same device state. |
| SponsoredData | POST | /campaign/management | manageCampaign | x | method-only | M1 | method=POST | w | manageCampaign only toggles the campaign between "PAUSE to temporarily halt new sponsorships" and "RESUME to reactivate the campaign," a status-set operation with the same end state however often it is applied. |

## Negative controls

- ClickToDial DELETE /calls/{callId} `terminateCall`: rule class = x, gt class = x — PASS
- WebRTC PUT /sessions/{mediaSessionId}/status `updateSessionStatus`: rule class = w, gt class = x — FAIL
