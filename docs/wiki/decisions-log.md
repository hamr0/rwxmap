---
type: reference
title: Decisions log
status: stable
---

# Decisions log

Source: verbatim copy of the decisions table (docs/archive/prd.md:649-698).

## 9. Decisions log — 2026-09-06

| # | Decision |
|---|---|
| D1 | Purpose: rwxmap is the author's own discovery tool first (maps r/w/x per operation for agents/guards/harnesses), a supporting but non-load-bearing PoC for -02's actionClass axis, and a stopgap map when a declared menu is absent or slow. |
| D2 | Classes `r < w < x`; safety spine is tighter-on-unknown, never loosen without evidence; changes the outline's "omit on unknown" (right for a verifier consuming a signed menu) to "answer with the tightest class" (rwxmap's consumers need an answer for every operation) — same rule, different consumer. |
| D3 | Two mechanical signals (HTTP method per RFC 9110; verb library lookup) and one arbiter: agree → that class, high confidence; disagree → tighter class, low confidence; verb unknown → method default, method-only confidence. No model/LLM tier. **Superseded by D17 (2026-09-06).** |
| D4 | Direction is both: propose loosening (POST → r/w) and tightening (DELETE/PUT → x for destructive verbs). Whether tightening enters the -02 argument is open, pending M0's tighten-case count. |
| D5 | Inputs: method, operationId, path, regex-only to start; OpenAPI shape rules added only in M2, only for a divergence class appearing more than once. |
| D6 | Output: class, confidence, rule id, evidence, plus MCP's four tool-annotation hints mapped from class and method. |
| D7 | Verb corpus: APIs.guru `openapi-directory` (CC0), priors not truth; no corpus size cited until queried live. **Demoted by D18 (2026-09-06).** |
| D8 | Test bed: the 292-operation, SHA-pinned CAMARA set copied to `data/camara-2026-09-01/` (origin `justabit:ietf/v3/poc/spike-a/operations.csv`); known GET-template and 57/138-reader-judgement limits stay stated. |
| D9 | Verb-signal strength varies by catalogue style (CAMARA rich, AIP `:verb` strong, Zalando verb-free weak); go/no-go is not "works everywhere." |
| D10 | Prior art: openapi-mcp is method-only (the gap rwxmap fills); OpenAPI has no operation-level safety field (open issue #2649); unfetched sources are not evidence of absence. |
| D11 | Home: this repo, GitHub remote pending; not a standards track, not normative, not a mandated harness; zero dependencies (`node:crypto` + vanilla JS). |
| D12 | Ground truth for the 292 is read by an agent, one reason per row naming the spec field; the user reviews the divergence list and a random sample of agreements. Decided 2026-09-06. |
| D13 | Test bed lives in this repo at `data/camara-2026-09-01/`: a verbatim copy of `justabit:ietf/v3/poc/spike-a/` (operations.csv, disagreements.md, scripts, README) plus the 92 fetched CAMARA YAML specs under `specs/`, so experiments run against files in this tree, never against justabit. |
| D14 | The r/w/x to MCP-hint mapping is written into §4.3 now as M3's starting table, marked untested. |
| D15 | Module discipline: one module at a time, each works on its own before the next starts; M0 is many numbered experiments, each with a readout, and the winning arbiter shape is picked on the two error counts. |
| D16 | `x` is read as consequence, not merely non-idempotence: an operation that reaches a third party, moves money, acts on a live session, network path or device, or cannot be undone is `x` even when repeating it is equivalent. This diverges from the -02 draft's literal definition of `x` and is M0's proposed correction to that text; see §4.4. Decided 2026-09-06. |
| D17 | Supersedes D3. The HTTP method sets a floor and, for safe methods only, also the ceiling; the operation's own text sets the ceiling elsewhere. Three signals, not two: method, text (`summary`/`description`), and structural markers (`callbacks`, `sink`, 409). Lowering the class is confined to POST and PATCH, where RFC 9110 guarantees nothing; it is never permitted on a safe method and never below `w` on PUT or DELETE. See §4.5. Decided 2026-09-06. |
| D18 | The verb-library plan (D7, M1) is demoted from the primary signal to one input to the text signal. M0 measured that a verb table alone cannot reach the gate: with both signals agreeing on 7 of the 11 remaining wrong loosenings, no table over paths and operationIds can see the consequence those rows carry. Decided 2026-09-06. |
| — | Outline superseded: the outline's three-tier model (deterministic / model / silence) and "no default class, omit on unknown" framing are replaced by D2 and, as of 2026-09-06, by D17 below — a floor from the method, a ceiling from the operation's own text, tighter-on-unknown, no model tier. |
| D19 | Second test set: `data/holdout-2026-09-07/` (Twilio, Stripe, GitHub; 207 ops; SHA-pinned; blind-read ground truth) is kept as a hold-out for honest scoring; CAMARA remains the build bed. Any shape tuned on the hold-out loses that status and the fact is recorded in learnings. Decided 2026-09-07. Confirmed by the user 2026-09-07. |
| D20 | The test for `x` is either of two roads, not one: the operation reaches beyond the caller (a person, another party's resource, money, a live session, network path or device), OR repeating it is not equivalent to doing it once. Either alone is sufficient; neither alone is necessary. Measured over the 499 labelled operations: of 153 `x` rows, 35 are `x` only because they are not repeatable, 43 only because they reach beyond the caller, and 75 for both — so a test resting on non-idempotence alone would miss 43 of 153, and 20 DELETE rows are `x` while being perfectly idempotent. Irreversibility is explicitly NOT the test: 129 DELETE rows are `w` and nearly all are irreversible, and the readers classed "permanently delete … cannot be undone" of the caller's own resource as `w`. Refines D16. Decided 2026-09-07. |
| D21 | DELETE, PUT and PATCH keep `w` as their default; the arbiter's rules raise from there. The alternative, defaulting to `x` with a declared menu as the only way down, was measured: on CAMARA it costs 17 more over-tightenings of 292 and removes no wrong loosening the rules had not already removed; on the blind hold-out it costs 49 more of 207 and collapses to the trivial "everything is `x`" (140 over-tightenings, agree 67 of 207). The floor stays `w` and the burden stays on evidence. Decided 2026-09-07. |
| D22 | The go/no-go's zero applies to high-confidence rows; the wrong-loosening rate among low-confidence rows is reported alongside it, never folded into it, and never traded away. This follows the same rule as the two error directions: separate counts, never one number. It is now measurable because pass 2 (E17) grades evidence without changing any class. Decided 2026-09-07. |
| D23 | Pass 2 grades evidence only; it never changes a class. Measured in E15 and E16: used as an un-raiser the same three checks created 8 wrong loosenings (3 from an over-generous artefact list, 5 on rows pass 1 had held for a fake reason); used only to mark evidence weak or strong they move nothing and let a consumer filter 53 of 78 hold-out over-tightenings without loosening anything. "This evidence is weak" is not evidence of safety. Decided 2026-09-07. |
| D24 | The clean exam `data/holdout2-2026-09-07/` (Box, PagerDuty, Adyen; 220 ops; 94 GET; SHA-pinned; blind-read; zero rulings) is the reference score for M0 and stays untouched by tuning: any change to a lexicon, verb table, party list or rule after 2026-09-07 is scored on it once and the fact recorded in learnings, and it is never used to choose between shapes. M0 closes with the gate (D22) met on all three sets at high and low confidence; the last clean-exam leak was closed by a post-exam list edit, recorded as taint in D27. Decided 2026-09-07. Confirmed by the user 2026-09-07. |
| D25 | The four-token cap in the verb-led object-head extraction is removed (learnings E21). Scored once on the clean exam per D24: one class changed (delete_shield_information_barrier_segment_members_id, w to x, truth x), no other row moved on any set. Decided 2026-09-07. |
| D26 | The three irreversibility stems in the word-list lexicon (permanent, irreversibl, cannot be undone) are removed; they encoded "irreversible means x", which D20 rejected. Scored once per D24 (learnings E22): eight rows move from x to w, all with truth w (CAMARA test 3, hold-out 1 1, clean exam 4); no row moves the other way; wrong loosenings unchanged. Decided 2026-09-07. |
| D27 | "collaboration(s)" is added to the verb-led party list at the user's request (learnings E23). Scored once per D24: one row changes, delete_collaborations_id w to x, truth x, the last clean-exam wrong loosening; no other row on any set moves. Stated taint: the word was added after the clean exam exposed it, so the clean exam's result on that one row is no longer blind; the other 219 rows are unaffected. The fix does not generalise (membership, assignment, share are the same shape), so the structural schema signal stays M1's first item. Decided 2026-09-07. |
| D28 | Two axes. r/w/x stays the blast-radius axis (D20: reaches beyond the caller, or not repeatable). A second, independent boolean, `destructive`, is derived from the method (DELETE) and the lead verb (`poc/m0/destructive.json`) and never from the class; it feeds MCP `destructiveHint`. Motivation: the user's case "read and reply, never delete" cannot be said with one ordered letter, because reply is `x` and `x` sits above `w`; and §4.3's untested mapping `destructiveHint = (class == x)` was measured wrong on 352 of 719 labelled rows (learnings E24). The proposal to move `x` to mean delete was rejected because pay, send, reply and add-a-stranger-to-admins would all become `w`. The destructive verb list is unmeasured against hand labels; measuring it is an M1 item. Decided 2026-09-07. |
| D29 | M0 is closed as exploratory, 2026-09-07. What it delivered and what M1 keeps: 719 blind-read labels across three sets (CAMARA 292, hold-out 1 207, clean exam 220), the scoring harness with the two error directions counted apart, the negative controls, the per-set reporting rule (D22, D24), and the findings about what the method and prose can and cannot see (D16-D20, E20-E25). What it did not deliver: a shape to keep. The E19 union arbiter, both hand lists, pass 2 and the E25 variants are archived under `poc/m0/` unchanged and are not promoted; they are the measured ceiling of prose-only rules (about 30 over-tightenings per 200 operations at zero wrong loosenings), not the design. M1 starts fresh in `poc/m1/`, borrows plumbing by import only (spec loader, split, CSV parser, scorer), and is an informed POC, not a build. Decided 2026-09-07 at the user's word. |
| D30 | M1 go/no-go set by spec interview: zero leaks among assigned rows; review plus over-tight near 5% per set on CAMARA and hold-out 1; remaining false flags must be humanly confusing; a field is primary only at ≥90% presence per method per set; leans from a broader corpus by method co-occurrence. See §4 M1. Decided 2026-09-07. |
| D31 | Truth fix: CAMARA queryAssistant (ModelAsAService POST /answer) moves x -> r at the user's word, matching Box post_ai_ask (r) in the clean exam. One row; CAMARA becomes r 156, w 51, x 85. The old label carried a stated doubt. Decided 2026-09-07. |
| D32 | Poll-then-callback reads are `r`. A callback raises to `x` only when the verb is not a read; "read" is decided mechanically (read-family scope token, or a corpus GET share >= 0.75 at >= 3 providers), never by a hand list. Three CAMARA rows (retrievePopulationDensity, retrieveConnectivity, count) keep their `r` label and move from over-tight to exact (M1-C7). Decided 2026-09-07 at the user's word. |
| D33 | M1's arbiter is two passes. Pass 1: the machine-facing fields (method prior, scope, body, callbacks, corpus lean, CAMARA verb table) with a confidence sum; assigns above the threshold. Pass 2: a verb-and-noun judge on the residual only, with orchestrator-written lists (live verbs, own verbs, party nouns) measured rule by rule and admitted only at zero leaks on CAMARA + hold-out 1; switches chosen by cost after zero leaks. The verb never gives `w` on POST. Set by the user's reading of the review pile 2026-09-07, measured M1-C9. Superseded by D34 (C11) on 2026-09-08. |
| D34 | M1 arbiter shape is C11 — floor + raise-only from fixed hand lists. Decided 2026-09-08. See "M1 arbiter flow (C11, adopted 2026-09-08)" in §4. |
| D35 | Rule A: a PUT/DELETE/PATCH with no summary and no description is x, marked no-text. Decided 2026-09-08. Hold-out 3 becomes a tuning set; hold-out 4 is the clean exam. |
| D36 | Shared-object nouns join the party-noun raise (message, channel, emoji, sticker, pin, guild, permission, overwrite, ban, webhook, reaction). Three vendors' readers drew the line at x. Decided 2026-09-08. |
| D37 | Per-vendor extension keys are rejected as a signal. GitHub's `x-github.triggersNotification` was measured — true on 3 of 81 labelled GitHub rows, all truth x, absent on 78, true on 22 of ~1225 whole-spec operations, a zero-leak raise that would have closed 2 of the 4 remaining leaks — and still refused: a rule that names one vendor is per-API maintenance, not a rule. Signals must be commonalities that hold across vendors. Leaks stay at 4. Decided 2026-09-08 at the user's word. |
| D38 | The benchmark reports against three dumb baselines (all-x, get-else-x, method-prior) and splits the five sets into tuned / reference / clean-exam, with the headline quoted from the clean exam only. One re-runnable command, `node poc/m1/arbiter/run-benchmark.mjs`. Decided 2026-09-08. |
| D39 | Hold-out 5 (Slack, Notion, Amazon SP-API; 323 rows; `data/holdout5-2026-09-08`) scored once 2026-09-08; the M1 go/no-go gate (D30, zero leaks) FAILS on it with 17 leaks, all Slack GET rows. Three root causes: c11's rule 1 locks GET/HEAD/OPTIONS to `r` before any text is read; the text rules read `summary` while Swagger 2.0 vendors (Slack, Amazon) fill `description` instead; `naiveSingular`'s `es`-stripping rule destroys third-person verb stems (revokes->revok, exchanges->exchang). No fix applied in this pass. |
| D40 | Hold-out 5 is reclassified from clean exam to TUNING set. Eight switch configurations were measured against it in M1-C13b, which is using it to choose — forbidden of a clean exam by D24. Every set bucket table (docs/logs/m1/benchmark.md, README) that lists hold-out 5 must now bucket it as tuned. Hold-out 6, not yet built, becomes the next clean exam. |
| D41 | M1-C13 fix C (verb-correct stem matching in judge.mjs, covering bare/-s/-es/-d/-ed/-ing plus silent-e drop, consonant+y -> -ies/-ied, and CVC doubled-consonant forms) is adopted as the default matcher for LIVE_VERBS and READ_VERBS on both the operationId and summary paths. Measured inert: zero rows changed on all six sets. Fixes A (read GET) and B (description fallback) were measured but NOT adopted in that pass. The noun sets still use naiveSingular and were not fixed. |
| D42 | Each HTTP method's floor is set at its own measured truth lean over the 1478-row labelled corpus, and the floor is a starting value, never an early return: GET/HEAD/OPTIONS -> r (truth 97% r), POST -> x (62% x), PUT -> w (83% w), DELETE -> w (81% w), PATCH -> w (69% w). PATCH moves from x to w; all other floors are unchanged. Measured 2026-09-09. |
| D43 | Word rules are scoped per method, one direction of travel each, replacing c11's blanket scan across all methods: GET/HEAD/OPTIONS run no word rules; POST lowers only; PUT/DELETE/PATCH raise only. Vocabulary is split to match — a lowering list used only on POST, a raising list used only on PUT/DELETE/PATCH. Measured over 1478 rows: exact 82.6% -> 84.4%, over-tight 16.0% -> 13.8%, leaks 1.4% -> 1.8% (the six new leaks are all PATCH, from the D42 floor move). |
| D44 | rwxmap emits no confidence score. The only honest per-row signal is evidence vs floor — whether a word rule actually read the operation and fired. Measured: all 27 leaks are floor rows; in 208 evidence rows there are zero leaks. The per-API report therefore carries class counts, the evidence/floor split and a review-first list of floor rows on PUT/DELETE/PATCH, and carries no accuracy, leak or over-tight figure, because those need ground truth. Leak reporting and x-dressed-as-w labelling belong to the benchmark only. |
| D45 | The labelled corpus is consolidated at data/corpus/labelled.csv (1478 rows, 37 columns), generated by poc/m1/corpus/build-labelled.mjs, read by loadLabelledCorpus(). It is the single source for readings from here on. The existing scattered loaders in load-sets.mjs stay in place and unchanged. The APIs.guru per-token lean table survives at docs/logs/m1/corpus-leans.csv (3688 tokens, position lead/opid, provider counts and per-method counts, hold-out vendors excluded at extract time) and is sufficient for a per-method word-lean scan. What was not persisted is the raw per-operation dump and the downloaded specs, which extract.mjs wrote to a session scratchpad since wiped; a re-extract is needed only for questions the aggregate cannot answer, such as reading operation summaries or word co-occurrence. |
| D46 | Exam methodology, set once for exam 1, 2 and 3: each is drawn blind from data/corpus/apis-guru-ops.csv.gz from providers appearing in none of the six original labelled sets, as a separate blind paper (no class column) plus a separate answer key; labelling agents were forbidden to open any classifier and could answer '?' rather than guess. Draws are reproducible (seeded PRNG, one script per exam): exam 1 (data/exam-2026-09-09/, 200 rows, 105 providers, 199 scorable) via poc/m1/arbiter/make-exam.mjs, seed 20260909; exam 2 (data/exam2-2026-09-10/, 1000 rows, 246 providers, 994 scorable) via make-exam2.mjs, seed 20260910; exam 3 (data/exam3-2026-09-11/, 3000 rows, 305 providers, 2993 scorable) via make-exam3.mjs, seed 20260911. Together with the original 1478 rows the labelled corpus reaches 5465 rows across 332 providers. Decided 2026-09-09. |
| D47 | Attack order for the three M1 goals (PRD §"The three goals") is goal 2 (x dressed as w), then goal 1 (w dressed as x), then goal 3 (r dressed as x or w); GET is last. One goal at a time — while a goal is open, other goals, other methods and other vendors stay out of focus. Decided 2026-09-09 at the user's word. |
| D48 | Goal 2's shape is the via-negativa allowlist (M1-C20): learn an allowlist of "yours" nouns and raise a PUT/DELETE/PATCH floor row to x when none of its head nouns are on the list, rather than learning a blocklist of third-party nouns (which C16-C19 measured does not transfer under leave-one-vendor-out). Adopted at the LOOSE bar, n>=2 rows and minW 0.80 w-share, 439 words. Measured leave-one-vendor-out over the 5465-row combined corpus: 89 leaks (1.6%) vs c15's 297 (5.4%), same 522 real over-tight rows, flagged-unknown rows rising from 152 (2.8%) to 1397 (25.6%). C20 is a POC (poc/m1/arbiter/c20.mjs), not shipped. Decided 2026-09-09 at the user's word. |
| D49 | Leaks in the 2-4% range are acceptable to the user, given that every leak is flagged (D44's evidence-vs-floor split names every review-worthy row; C20 measured that leaks have landed exclusively on flagged floor rows in every pass run so far, 297 of 297). Decided 2026-09-09. |
| D50 | Contested words that sit in both the general C20 candidate pool and c11.mjs's hand-written PARTY_NOUNS (network, device, person, customer, contact, partner) are settled by measured lean over PUT/DELETE/PATCH rows, not by taste. All six measured as leaning "yours" (w), 83.3%-100% w-share; three agreed with the user's prior expectation (device, person, contact) and three disagreed (network, customer, partner, which the user expected to read as third-party). This has no scored effect yet, since c15's PARTY_NOUNS rule fires before a row can reach the C20 layer — acting on it means editing PARTY_NOUNS in c11.mjs, which remains open. Decided 2026-09-09 at the user's word. |
| D51 | Goal 1's shape (w dressed as x) is the `allowlist-wins` layer of M1-C22 — where c15's hand-written LIVE_VERBS/PARTY_NOUNS/SHARED_NOUNS rules raise a PUT/DELETE/PATCH row to x, but every one of the row's cleaned head nouns is on C20's measured "yours" allowlist, the row is lowered back to w with rule `allowlist-wins`. Adopted at variant N (party-noun rule only, not live-verb) with the TIGHT bar minN=5 and minW=0.95, a 106-word allowlist — deliberately tighter than goal 2's loose bar (D48) because this is the project's first rule that loosens. Measured leave-one-vendor-out over 5465 rows: 61 of 522 over-tight rows rescued at a cost of 6 new leaks; against the 5465-row base, false alarms fall 9.6% to 8.4% and new misses rise 0 to 0.11%. Adoption is conditional on flagging: every row lowered by this rule is marked review, never confident, preserving the standing property that the tool is never confidently wrong in the loosening direction (D49's flagged-leak reasoning extended to the loosening direction). Path-tail nouns (C23) and description markers were measured and rejected. C22 is a POC (poc/m1/arbiter/c22.mjs), not shipped. Decided 2026-09-10 at the user's word. |
| D52 | PARTY_NOUNS and SHARED_NOUNS stay as they are. The via-negativa cleanup (M1-C24) was measured under the user's advance ruling that any rise in goal-2 leaks means leave the list alone. On goal 2's own ledger (c15 + C20, C22 off, LOVO, 5465 rows), removing the 9 "yours" words raises goal-2 leaks from 89 to 98 (+9), and keeping only the 14 proven third-party words raises them to 106 (+17); the new leaks include "Delete a customer" and "Delete a repository", both high confidence. Combined (c15 + C20 + C22): control 95, KEEP 99, STRIP 107. This closes D50's open item with a measured no. The same pass corrected two recorded baselines: goal-2 for bare c15 is 280 (5.1%), not 297 — 297 is all loosening including 17 GET rows, mislabelled as goal-2 in D48, D49 and the PRD; goal 2's own count is 89 (frozen since D48), goal 1's C22 adds 6 leaks charged to goal 1's ledger, and 95 is the combined line, not goal 2's count; and today's full-stack false alarms are 1875 (34.3%), of which 1311 come from C20's no-own-noun raise, because goal 1's 461 and goal 2's 89 had each been measured with the other layer off. "Flagged" is not yet wired in code (C20 raises carry floor:false). Decided 2026-09-10 at the user's word. |
| D53 | Exam 4 is drawn under D46's methodology — data/exam4-2026-09-12/, 4000 PUT/DELETE/PATCH rows from 318 providers, seed 20260912, via poc/m1/arbiter/make-exam4.mjs, zero provider overlap with the 332 burned providers (computed, the script throws otherwise), reproducible byte-for-byte. It is unlabelled; labelling is the gate on scoring any further rule change, including re-scoring goal 1's C22 layer on virgin rows. Decided 2026-09-10 at the user's word. |
| D54 | Goal 2 rebuilt clean in poc/m1/goal2: floor -> verbs -> yours-nouns, reading every noun token of the operation name (verbs stripped). The hand-written third-party lists PARTY_NOUNS and SHARED_NOUNS are not part of this shape. Goal 2 FROZEN at 37 leaks (4.0% of 936 truth-x), all floor rows, down from 89. Provisional: a clean exam may move it. Supersedes D48's two-head-noun form; D52's keep ruling applied to the old shape only. Decided 2026-09-12. |
| D55 | Goal 1 REOPENED at 2727 false alarms (was 1936; +791 is the charged cost of goal 2's freeze, on goal 1's ledger). D51 / M1-C22 is void: the party-noun rule it lowered back no longer exists. Decided 2026-09-12. |
| D56 | Code layout poc/m1/{core,goal1,goal2,goal3,run}; one fixed order in run/pipeline.mjs (verbs -> goal 2 -> goal 1 -> goal 3); each layer one direction, the pipeline throws otherwise; each goal's number pinned by its own test in run/ledger.test.mjs, moved only by a recorded ruling. The old poc/m1/arbiter/c*.mjs pass files are frozen history. Commit db04f24. |
| D57 | Each goal owns its own word lists and never imports another goal's list; it may copy entries that genuinely apply. Core holds only what truly works shared: the floor and the operationId splitter, no lists. Goal 2 owns LIVE_VERBS (26) and the yours-noun allowlist (439); goal 3 owns READ_VERBS (14); goal 1 builds its own lists fresh. Moving the lists out of core's imports is a later step, not done yet. Decided 2026-09-13 at the user's word. |
| D58 | The operationId splitter moves into core and also splits on `/` and whitespace (the old one in arbiter.mjs split only on `_ - .` and camelCase, so `gists/unstar` was one token). It reaches the verb rules, the noun set and the allowlist build. Measured LOVO over 5465 rows: 176 rows touched in 11 vendors; goal 2 37 unchanged; goal 3 49 unchanged; goal 1 2727 -> 2703; the old-shape gate still reproduces 89. Goal 1's pin moves 2727 -> 2703 at the user's word. Decided 2026-09-13. |
| D59 | MCP `readOnlyHint` for GET/HEAD/OPTIONS follows the class — floor r, so readOnlyHint true. Measured cost: 17 of 550 GET rows wrong (13 truth x, 4 truth w, 3.1%). No code emits hints yet (M3). Parked while goal 1 is open. Decided 2026-09-13 at the user's word. |
| D60 | Goal 1's layer adopts a lowering-verb rule with its own mined leave-one-vendor-out list (poc/m1/goal1/lists.mjs): a PUT/DELETE/PATCH row still at x whose summary lead verb (trailing 's' folded) is admitted — excluding the row's own vendor, >=5 other vendors and non-x share >=0.95 — lowers to w. Today admits PUT enable, PUT mark, PUT edit, PUT save. Every lowered row is flagged (floor:true). Measured leave-one-vendor-out over 5465 rows: 53 false alarms fixed, 0 new leaks. Goal 1's pin moves 2703 -> 2650; goal 1's leak cost stays 0. Decided 2026-09-13 at the user's word. |
| D61 | Goal 2's noun reader (poc/m1/goal2/allowlist.mjs, nounsForRow) stops treating path placeholders (`{id}`, `{app`), version tags (`v1`, bare digits) and filler words (from, using, or, and, by, for, to, of, the, a, an, with, in, on, at, into, via, all) as nouns, via a new `isJunkToken` check applied in both extraction loops (head nouns and remaining tokens). Measured in a copy before the edit, then reproduced on the real code, leave-one-vendor-out over 5465 rows: goal 2 unchanged at 37 leaks (the freeze, D54, stands); goal 1's false alarms fall 2650 -> 2592, 58 rows freed, 0 new leaks charged to either goal; goal 3 unchanged at 49. The allowlist TABLE build (buildNounTable, poc/m1/arbiter/c19.mjs) does not go through nounsForRow — it extracts candidate nouns only via headNounForRow + operationIdHeadNoun, so this fix changes what a row's own nouns look like at classify time (goal2.mjs's yours-noun check) but not which nouns get admitted onto a vendor's allowlist. Goal 1's pin moves 2650 -> 2592; goal 1's leak cost stays 0. Decided 2026-09-13 at the user's word. |
| D62 | Goal 1's layer adds piece 4, its own mined yours-noun list (poc/m1/goal1/lists.mjs, buildGoal1YoursNouns): after the lower-verb rule (piece 3, D60) runs and does not fire, a PUT/DELETE/PATCH row still at x has its noun set read with goal 2's nounsForRow (the reader, not a list) and lowers to w, flagged, when every noun is on goal 1's own leave-one-vendor-out yours-noun list — goal 1's own bar (2 vendors, 90% safe), looser than goal 2's raising bar (D48, n>=2/w-share>=0.80) because the direction and the ledger it is charged to are both goal 1's own (D48's bar is not reopened). Bar sweep on the piece-3 pile (2489 truth-w, 568 truth-x): 85% -> 192 freed/19 leaks, 90% -> 70/7, 95% -> 55/4, 100% -> 53/4; 90% adopted. First measured unrestricted (any prev.rule): 70 false alarms fixed, 7 new leaks — but 5 of those 7 were rows goal 2 had raised on a LIVE VERB, not a noun rule (amazon cancelShipment, appveyor.com cancelDeployment, getgo.com cancelRegistration, ticketmaster.com patchEvent "Publish a patch on an event", ote-godaddy.com's unnamed DELETE "Cancel the most recent user action for the specified domain"), meaning a noun list was overriding verb evidence. Restricted (the user's ruling) to fire only when prev.rule === 'no-own-noun' — noun evidence undoing noun evidence, a live-verb raise stands untouched. Restricted measurement, leave-one-vendor-out over 5465 rows: 61 false alarms fixed, 2 new leaks (fulfillment.com PUT put-returns "Inform us of an RMA", lufthansa.com PUT "Auto Check-In"), both flagged (D49 accepts flagged leaks, charged to goal 1's own ledger, never goal 2's). Goal 1's pin moves 2592 -> 2531; goal 1's leak cost moves 0 -> 2. Decided 2026-09-13 at the user's word. |
| D63 | Goal 1 is rebuilt as a standalone classifier with its own flow — floor -> its own live-verb list (a literal copy of goal 2's 26 words, D57: copy, never import) -> its own other-party noun list (mined leave-one-vendor-out over ALL PUT/DELETE/PATCH rows: a noun is someone else's for vendor v when, excluding v's own rows, it spans >=2 other vendors with a danger, truth-x, share >=0.30) — not a layer patching goal 2's output. Goal 1's ledger is scored only over the 4406 PUT/DELETE/PATCH rows (3776 truth w, 605 truth x), the same population its own classifier classifies; the corpus's other 103 truth-w rows are POST rows sitting at the POST floor's own default (x), which is the POST floor's mismatch, not goal 1's — classifyGoal1 returns every other method's untouched floor and never assigns those rows a class of its own. Measured: 803 false alarms / 211 leaks at the adopted bar (other vendors>=2, danger share>=0.30); bar sweep 40% -> 472/251, 50% -> 268/382. Goal 2 stays frozen and separate at 2645/37 (goal-1-false-alarms-at-goal-2-stage/goal-2-leaks, unaffected by this rebuild). The lower-back design of D60-D62 (2727 -> 2531 false alarms, restricted to undoing only goal 2's own raises) is retired: it could only ever undo what goal 2 raised, so it inherited goal 2's own curve rather than answering the question on its own evidence. Combining goal 1 and goal 2 into one answer is a later decision, not made here. Decided 2026-09-13 at the user's word. |
| D64 | Goals renamed to steps, named by the class each step claims: step 1 r (was goal 3), step 2 w (was goal 1), step 3 x (was goal 2). Pipeline reordered to floor -> step 1 -> step 3 to read like the flow; zero row change (all four proof CSVs identical, sorted). x is the byproduct of steps 1 and 2, not a step with rules of its own. Older decisions keep their original wording; read 'goal 3' as step 1, 'goal 1' as step 2, 'goal 2' as step 3. Decided 2026-09-13. |
| D65 | The classifier is rebuilt in poc/flow/ with zero imports from poc/m1/ or poc/m0/, one flow step 1 -> step 2 -> floor x, one noun reader for both mined lists, the yours list mined over write rows only (every method but GET/HEAD/OPTIONS — GET carries no yours signal, since a GET is truth r no matter whose thing it touches, and only dilutes the w-share). Trade measured: over all rows the pile is 2441 / 192 leaks; over write rows 1998 / 179 (adopted, the user's option 2); over PUT/DELETE/PATCH only 1582 / 159; classes (803 false alarms / 211 leaks) identical in all three, only the flag pile moves. The no-evidence flag is renamed "x-pile" (class stays w). One proof CSV, run-proof/flow.csv; every pin lives in poc/flow/ledger.mjs. Classes proven identical to the previous code on all 5465 rows before the switch. poc/m1/ to be archived. Decided 2026-09-14 at the user's word. |
| D66 | Two hand raisers adopted in poc/flow/step2.mjs, after the mined lists and above the 10-for-1 admission line: "confirm" added to LIVE_VERBS (27 words) and a new MONEY_NOUNS = {payment} list, checked after the other-party noun check — any noun in MONEY_NOUNS raises the row to x, rule money-noun (money moves by D20). Measured before adoption (learnings, "Between-bars words"): "confirm" alone closes 19 leaks for 2 false alarms (23 write rows, 3 vendors, all leaks from one vendor, climatekuul.com — transfer unproven); "payment" alone closes 11 leaks for 0 false alarms (68 write rows, 5 vendors); together, 24 leaks closed for 2 false alarms, 12 per 1, above the 10-for-1 line (D63's sequential-vs-joint measurement). Other money nouns measured and rejected: invoice 3 x / 15 w, transaction 1/23, charge 0/3, checkout 0/1, payout 1/1, fund too thin (one vendor). Leave-one-vendor-out over 5465 rows: step 2's pins move 803/211 -> 805/187; x-pile 1998/179 -> 1972/155; whole flow 78.4%/4.2%/17.5% -> 78.8%/3.7%/17.5%. Decided 2026-09-14 at the user's word. |
| D67 | The truth standard for exam 5 and later exams is data/calibration-2026-09-14/BRIEF.md, a labelling brief calibrated to the corpus standard (the user's option 2, 2026-09-14), replacing exam 4's reconstructed stricter brief. Measured blind against exam-3 truth: 185 of 200 random rows under the first version (exam 4's brief 177 of 200 on the same rows), 185 of 200 fresh rows after four fixes and a Methods paragraph; it leans slightly loose on truth-x rows (9 of 23). Its POST and GET guidance was not calibrated. Decided 2026-09-14 at the user's word. |
| D68 | Exam 5 (data/exam5-2026-09-14, 2000 rows, seed 20260914, poc/exam/make-exam5.mjs) is row-disjoint, not vendor-disjoint, for PUT/DELETE/PATCH, because the APIs.guru pool has no unseen write vendors (327 providers have write rows; 3 are outside the corpus). Its 1500 write rows come from 103 corpus vendors and are scored leave-one-vendor-out keyed on the exact corpus vendor name; its 300 POST and 200 GET rows come from 164 unseen vendors. A new spec source outside APIs.guru is the fallback if a row-disjoint exam cannot be drawn or scored honestly. Scored once 2026-09-14: 68.4% exact / 3.2% leaks / 28.4% over-tight overall; writes 73.3 / 3.7 / 23.0; POST 25.1 / 0.7 / 74.2 (learnings, "Exam 5 scored once"). Nothing adopted. Decided 2026-09-14 at the user's word. |
| D69 | Mining the "yours" allowlist from description text (poc/desc-yours/, a new POC folder, live flow untouched) is rejected as a way to shrink the x-pile. Leave-one-vendor-out sweep of source (summary/description/both) x minWShare (0.80/0.85/0.90/0.95) x minN (2/3/5): summary source resolves 0 rows at every setting (mechanically guaranteed — the pile is exactly the rows that already failed that bar); description, minN 2, 0.80 resolves 130 of 1972 rows (6.6%), 122 right, 8 leaks; description, minN 3, 0.80 resolves 93, 1 leak; both, minN 2, 0.80 resolves 71, 2 leaks (no single vendor carrying more than 8 of the 71); both, minN 2, 0.85 resolves 35, 0 leaks; a union variant (description nouns checked against summary-mined ∪ description-mined, minN 3/0.80) resolves 100, 2 leaks. Best case is a 6.6% dent leaking 8; safe settings clear 2-5%. This is the second mining approach in a row to fail to cross vendors (after the corpus-derived raise list, M1-C15, and the mined blocklist, M1-C12/C9): vendors do not share the words that matter. Decided 2026-09-16 at the user's word. |
| D70 | The core (poc/flow/) is FROZEN as of 2026-09-16: no word list or rule changes until the user lifts the freeze. Frozen numbers: 78.8% exact / 3.7% leaks / 17.5% over-tight on the corpus under LOVO; exam 5 70.2 / 3.2 / 26.7. The x-pile holds 1972 rows (36.1% of the corpus); inside it, 91.0% truth w, 7.9% flagged leaks (155), 1.1% truth r; 49 leaks (0.9% of rows) are unflagged. The pile stays as the flag, not a rule, because it catches 155 of the run's 204 leaks (D69). Next candidate work, not started: step 1's POST read-verb list. Decided 2026-09-16 at the user's word. |
| D71 | The labelling brief's POST paragraph (data/calibration-2026-09-14/BRIEF.md, commit d3a8ee9) was revised: a POST that reads is r; a POST that creates is x by road 2 (not repeatable), overriding rules 1, 4, 6 and 11 for POST only; a POST that edits an existing item the caller already owns is w; road 1 / road 2 actions are x — replacing the old sentence that called a POST create w and disagreed with corpus truth. The pass bar was pre-registered before any labelling (data/calibration-2026-09-15/README.md): practice must reach >=85% agreement before the holdback is touched, and the holdback PASSES only if agreement >=85% AND the sheet's x-share lands within 10 points of corpus truth. Practice (commit af2274b): 339 measure rows relabelled blind, 290/339 (85.5%) agree, x-share 57.2% vs corpus 60.5% (old brief on the same rows: 195/339, 57.5%, x-share 22.4%). Holdback, scored once (commit a385fae): 145/170 (85.3%) agree, x-share 60.6% vs corpus 66.5% (gap 5.9) — PASS on both pre-registered conditions, thin; the holdback is now burned, no unlabelled corpus POST rows remain. Consequence (commits 5073dab, ac8ee4a, f575b7e): exam 5's 300 POST rows were relabelled blind under the fixed brief and re-scored once — new POST truth r 189 / w 40 / x 70 / ? 1 (was r 188 / w 75 / x 36); POST scored 36.8 exact / 0.3 leaks / 62.9 over-tight (was 25.1 / 0.7 / 74.2); the whole exam moved to 70.2 / 3.2 / 26.7 (was 68.4 / 3.2 / 28.4); write and GET numbers unchanged; nothing in the classifier was tuned. Standing rule: every HTTP method a labelling brief covers needs its own calibration against corpus truth before any exam result on that method is trusted. Decided 2026-09-16. |
| D72 | GitHub push protection (GH013) rejected the push of branch m1/informed-arbiter over two vendor-published example credentials (an AWS access key ID and a Slack bot token) inside third-party OpenAPI spec files committed under data/holdout5-2026-09-08/specs/ — 9.6 MB of raw vendor downloads that no code reads (the classifier reads only ground-truth.csv and operations.csv, poc/flow/corpus.mjs:19-21). Toggling secret_scanning_push_protection off did not lift the block within a usable time and the GitHub unblock URLs 404'd; git-filter-repo could not be installed, so git filter-branch --index-filter removed the path data/holdout5-2026-09-08/specs from all 84 commits of the branch. Verified: 84 commits before and after, 0 dropped, 0 empty, 0 subject mismatches, 0 occurrences of the specs path anywhere in branch history, 0 blobs containing either secret, 66 poc/flow tests plus 6 poc/desc-yours tests pass, poc/flow/proof.mjs prints "All pins hold." with every number unchanged (x-pile 1972 / 155 leaks, whole flow 78.8% exact / 3.7% leaks / 17.5% over-tight). Standing rule going forward: raw vendor spec downloads are not committed to this repo — only the extracted rows (operations.csv, ground-truth.csv) and a README recording provenance are; data/camara-2026-09-01/specs/ is deliberately NOT affected by this rule, since CLAUDE.md names it as the project's test bed. Decided 2026-09-16. |
| D73 | The `rwxmap` name is reserved on npm at 0.1.0 (package.json, LICENSE, CHANGELOG.md, commit 42f1577). This is a name reservation only: no main/exports/bin, nothing importable, and the tarball ships exactly README.md, CHANGELOG.md and LICENSE (verified: 4 files, 7030 bytes) — the classifier stays a POC in poc/flow/ and is deliberately not shipped, per "never ship the POC". The repo is now licensed Apache-2.0, its first licence; there was none before. README's license section, which said "LICENSE file to follow," is fixed to point at the file. Decided 2026-09-16. |
| D74 | Step 1 (the r step, `poc/step1/`) is FROZEN as of 2026-09-16: no rule or word-list change until the user lifts the freeze. The frozen shape is three rules — (1) `method`: GET / HEAD / OPTIONS -> r; (2) `read-verb`: a POST whose lead token, after skipping the modifier words `bulk` / `batch` / `deprecated` / `beta` / `async`, stem-matches one of the 23 `READ_VERBS`; (3) `read-verb-anywhere`: a POST the lead rule did not claim, carrying one of the 10 `SAFE_VERBS` (a never-a-noun subset of the 23) at any token position. Ledger over all 4171 corpus rows: `method` 1960 claimed / 1958 right / 2 leaks; `read-verb` 87 / 87 / 0; `read-verb-anywhere` 5 / 4 / 1; total 2052 claimed / 2049 right / 3 leaks. Against the corpus's 2082 truth-r rows, 2049 are found (98.4%) and 33 missed. Leave-one-vendor-out, both word lists rebuilt per holdout: 83 claimed / 82 right / 1 leak. The user accepts the 3 leaks, extending D49's flagged-leak acceptance to step 1: `datadog GetGraphSnapshot` (truth x, low confidence) and `intercom listContactBanners` (truth w, low confidence), both on the GET floor and out of a method floor's reach by construction, plus `stripe PostPaymentMethodDomainsPaymentMethodDomainValidate` (truth w, low confidence), the one leak the user deliberately bought. `read-verb-anywhere` is the tool's first deliberate loosening, priced before adoption at 4 right rows for 1 leak; the LOVO reading is plain — the 4 gains are digitalocean's and the leak is stripe's, so on an unseen vendor the cost generalizes and the gain does not, and the user took the trade with that stated. Measured and rejected on the way: `verify` (4 fires, 3 wrong); `simulate`, `convert`, `test`; the full 23-word list read at any token position (31 leaks instead of 0, because `list`, `get`, `count` and `check` are object nouns as often as verbs); the summary-verb fallback; and the first-k-token window (k=2 costs 21 leaks for 7 rows, 20 of the 21 from mailchimp's `postLists*` naming). Two misses are left standing: `stripe PostTaxCalculations` and `PostRadarPaymentEvaluations` are truth r but tokenize to the noun forms `calculations` / `evaluations`, which the verb stemmer does not reach. The corpus contains no HEAD or OPTIONS rows, so those two methods are carried on principle, not evidence. Decided 2026-09-16 at the user's word. Commits 3519310, 653da3a. |
| D75 | Step 2 (the w step, `poc/step2/`) is FROZEN as of 2026-09-17: no rule or word-list change until the user lifts the freeze. It imports from `poc/step1/` (live adjacent code) and nothing from `poc/flow/` or `poc/archive/`; 17 tests, `node poc/step2/proof.mjs` prints "All pins hold.", proof CSV `run-proof/step2.csv`. The frozen shape is three rules — (1) `method-floor`: every PUT / DELETE / PATCH row starts at w, no words involved; (2) `modify-verb`: on a POST step 1 left behind, a verb from `MODIFY_VERBS` (24 verbs that act on a thing that already exists) at the operationId's lead token lowers x to w; (3) `modify-verb-summary`: the same list read against the summary's lead verb instead, when the operationId's lead token is a bare HTTP method word (stripe's `PostTaxCalculations`, mailchimp's `postLists`). Rules 2 and 3 are both BLOCKED when any word of the row is in `OTHER_PARTY` (21 role nouns), and the row then passes to step 3. Step 2 never assigns r or x. Ledger over the 2119 rows step 1 leaves it: `method-floor` 902 claimed / 836 right / 66 leaks; `modify-verb` 114 / 113 / 1; `modify-verb-summary` 118 / 114 / 4; total 1134 claimed / 1063 right / 71 leaks (6.3% of claims). It claims 53.5% of what it inherits and passes on 985 rows (46.5%, truth r 33 / w 153 / x 799), with zero over-tight rows of its own. The POST rules alone: 232 claimed / 227 right / 5 leaks — 2.2% fitted, 3.2% leave-one-vendor-out (219 claimed / 212 right / 7 leaks) with BOTH lists rebuilt per holdout, which is the honest figure; they reach 227 of the 380 truth-w POST rows (59.7%), against zero before. The user accepts the 71 leaks, extending D49's flagged-leak acceptance to step 2 as D74 did for step 1. The 5 POST leaks, named: `stripe PostClimateOrdersOrderCancel`, `stripe PostPaymentIntentsIntentCancel`, `stripe PostSubscriptionsSubscriptionExposedId`, `square BatchChangeInventory`, `mailchimp postCampaignsIdActionsCancelSend`; four of the five are the verb `cancel`, which keeps its place because it also brings 34 right rows. The other 66 of the 71 leaks are the wordless method floor, so step 2 cannot be improved by better words — only by step 3 raising those rows to x; they cluster in openai and zoom account administration (`delete-user`, `remove-group-user`, `unassign-project-user-role`, `userPassword`, `userSSOTokenDelete`), not in dangerous-sounding verbs. Whole flow with step 1, both frozen, plus the x placeholder, n=4171: exact 93.8%, leaks 1.8% (74 rows), over-tight 4.5% (186 rows); emitted-class precision r 2052 rows 99.9% right / 3 loose-wrong, w 1134 rows 93.7% / 71 loose-wrong, x 985 rows 81.1% / 0 loose-wrong. Measured and rejected on the way: the verbs `set` (3 leaks), `attach` (2) and `finalize` (1); a via-negativa mined yours-noun allowlist, which this 15-provider corpus structurally cannot feed (36 mined nouns against the old corpus's 439 from 332 vendors; best settings claimed 9 rows of 380 at 0 leaks, or 41 at 12% leaks); a list-free structural gate on a preposition linking two nouns (caught 8 of 22 leaks, cost 32 right rows); matching the read-verb list at any token position (31 leaks instead of 0); five `OTHER_PARTY` words written from imagination and appearing nowhere in the corpus (guest, guests, invitee, teammate, attendee), deleted before adoption; and, at the user's word on 2026-09-17, four more gate words — `customer`, `contact`, `agent`, `person` — that vetoed 18 rows between them (all 18 truth w) and caught zero leaks, whose removal moved exactly 16 rows from x to w, all truth w, with leaks unchanged at 5. Also measured and rejected: running `OTHER_PARTY` as a RAISER over the 902 PUT/DELETE/PATCH floor rows to catch the 66 floor leaks — 0.31 leaks closed per false alarm as built, 0.50 trimmed, against the project's adoption bar of 10, because `user` flags 42 write rows of which only 13 are truth x; the single exception is `permission`, 6 flags, all 6 truth x, 0 false alarms, left for step 3 to rule on and not adopted here. The honest limits stand: LOVO is concentrated, with stripe alone carrying 92 of the pre-trim 204 claimed rows and 3 of the 7 leaks while six of the fifteen providers claim zero rows under LOVO, so 3.2% is really a stripe/openai/jira/square number; and there is no clean exam — steps 1 and 2 were both measured on the same 4171 rows they were tuned on, which under D24 makes this corpus a tuning set from now on. Decided 2026-09-17 at the user's word. Commit 1022ac7. |
| D76 | The output shape is AGREED as of 2026-09-17: rwxmap publishes no format of its own and does not become a standards track. It keeps one map file it owns — one row per operation keyed by method + path + operationId, carrying `class` (`r`/`w`/`x`), `destructive` (the separate D28 axis, never read off the class), `evidence` (the rule that claimed the row, with `floor-only` as the already-measured honesty flag) and `confident` — and fills the extension slot each existing standard already leaves open for third-party data. Four carriers: (1) OpenAPI, per operation, an `x-rwx` object on the operation, written back into the same file rwxmap read; (2) MCP, per tool, the four hints in the spec's own `annotations` object plus rwxmap's own fields under reverse-DNS `_meta` keys (e.g. `io.github.hamr0.rwxmap/class`), `_meta` being the extension slot named by the MCP 2026-07-28 specification, which reserves prefixes whose second label is `modelcontextprotocol` or `mcp`; (3) WebMCP, per tool, nothing custom at all — its `ToolAnnotations` dictionary (read 2026-09-17: `readOnlyHint`, `untrustedContentHint`, `consequentialHint`, `debugging`, defined members only, no `_meta`) carries all three classes in two flags, r = readOnlyHint true, w = both false, x = consequentialHint true; (4) Agentic Resource Discovery (Google and ten others, published 2026-06-17), per domain, ONE `/.well-known/ai-catalog.json` entry pointing at the map file by URL. Per-operation data provably cannot ride inside an ARD catalog entry: the published entry schema sets `additionalProperties: false`, offers no `x-` support and no extensions object, and its only free slot, `metadata`, accepts flat key-value pairs limited to string, number, boolean or null. The finding that decided the shape is WebMCP's `consequentialHint`, which is a closer fit to this project's `x` (reach beyond the caller, or not repeatable) than any MCP hint: `destructiveHint` was already rejected as a reading of the class (D28) and `idempotentHint` cannot be read off specs at all (D75), so WebMCP is the carrier where the r/w/x ladder maps with nothing left over — and `consequentialHint` IS step 3, which raises step 3's value beyond one MCP hint. Consequences: rwxmap is a build-time annotator, not a runtime component, so signing stays the Resource Owner's act per Out of scope; only two of the four slots are rwxmap's own (`x-rwx`, `_meta`); nothing emits yet and no emitter is built before step 3; what is shippable is unchanged — `readOnlyHint` in both MCP and WebMCP at 3 unsafe-wrong rows in 4171 (0.07%), with `consequentialHint` and `idempotentHint` waiting on step 3. All four standards were fetched and read on 2026-09-17 rather than recalled, and every field name, default and schema constraint above MUST be re-checked against the targeted revision before anything emits, because all four are moving: WebMCP is a W3C Web Machine Learning Community Group Draft Community Group Report of 2026-04-23 and ARD is a draft under Apache-2.0. Decided 2026-09-17 at the user's word. |
| D77 | The floor/list axis is named in both artifacts and D76's map shape is AMENDED, decided 2026-09-17 at the user's word. Every answer the tool gives comes from exactly one of two sources: a `floor` (the HTTP method alone, no word matched — a DEFAULT, which a later step's evidence may override, exactly as step 3's `raise-word` overrides step 2's `method-floor`) or a `list` (a word list fired — EVIDENCE, and final, which nothing downstream overrides). Measured over the 4171-row corpus: `list` 343 rows / 337 right / 6 leaks / 0 over-tight; `floor` 3828 / 3593 / 49 / 186. The split earns its place because the tool is mostly an HTTP method table — 3593 of its 3930 correct answers (91.4%) come from floors, while word lists fire on only 8% of rows though they are 98.3% right when they do — and because the two failure modes sit on opposite sides of it: 47 of the 55 leaks are in one cell (step 2's wordless PUT/DELETE/PATCH floor) and all 186 over-tight rows are in one other (step 3's `floor-post` pile). TWO ARTIFACTS, TWO AUDIENCES. (1) The internal debugging sheet `run-proof/step3.csv` now carries 12 columns — provider, method, path, operationId, summary, truth, confidence, class, step, rule, source, matched — where `source` is floor/list and `matched` is the specific word-list member that fired, joined with `+` in sorted order when several match, and EMPTY on every floor row because nothing matched. Both fields have one writer each in `poc/step3/step3.mjs` (`sourceForRule`, `matchedWordsForRule`), both throw on an unknown rule so a future rule cannot silently default, and `matched` is RE-DERIVED from the frozen steps' own exported helpers because steps 1 and 2 return only a rule name; six proof pins guard that re-derivation, the load-bearing one being that `matched` is non-empty for exactly the 343 `list` rows and empty for exactly the 3828 `floor` rows, so a re-derivation that misses a row fails the proof rather than printing a blank. The sheet is never published. It immediately paid for itself: the six leaks that fired a word are now readable straight off it — `cancel` 3 (stripe PostClimateOrdersOrderCancel, stripe PostPaymentIntentsIntentCancel, mailchimp postCampaignsIdActionsCancelSend), `validate` 1, `update` 1, `change` 1 — as is which words earn their place (of `read-verb`'s 87 rows, `search` 27 / `get` 16 / `retrieve` 15 do 67% of the work and ten words fire once or twice each). (2) The published map's `evidence` field, which D76 defined as the rule that claimed the row with `floor-only` as its honesty flag, now carries ONLY `floor` or `list`, and the `confident` field is REMOVED entirely because `evidence` already carries it — `floor` IS the unconfident case, and two fields saying one thing can contradict each other. Rule names and the matched word are deliberately unpublished: an adopter cannot act on them, and publishing them would freeze this project's internal naming into someone else's contract. The PRD's map example is now four real corpus rows showing what the tool actually emits, including one deliberately wrong row — openai `createChatCompletion`, truth `r`, emitted `x` with `evidence: "floor"` because nothing spoke for it — so a reader sees for themselves what a floor row is worth. Verification: 81 tests pass across the three steps, all three proof scripts print "All pins hold.", `poc/step1` and `poc/step2` and their proof CSVs are untouched and still frozen (D74, D75), and the orchestrator re-derived the floor/list split and the empty/non-empty invariant from the CSV itself rather than from proof.mjs. Commits 210464a and the one carrying this row. |
| D78 | Step 3 (the x step, `poc/step3/`) is FROZEN as of 2026-09-17: no rule or word-list change until the user lifts the freeze. It imports from `poc/step1/` and `poc/step2/` (live adjacent frozen code) and nothing from `poc/flow/` or `poc/archive/`; 19 tests of its own, 81 across the three steps, `node poc/step3/proof.mjs` prints "All pins hold.", proof CSV `run-proof/step3.csv`. With this the ladder is complete for the first time — three real steps and no placeholder. The frozen shape is two rules plus one wiring change. (1) `raise-word`: a row that step 2 claimed with its WORDLESS method floor is raised from w to x when one of the 17 `RAISE_WORDS` nouns — permission, membership, memberships, panelist, panelists, watcher, watchers, participants, actor, invites, invitation, invitations, sso, password, grant, disassociate, reject — appears at ANY token position among the row's words, the word source being step 2's `wordsForRow` (operationId plus summary) PLUS the row's non-param path tokens, which is worth exactly one extra correct row over `wordsForRow` alone at no cost. They are role/access nouns, not verbs, although the PRD calls the mechanism "live verbs". (2) `floor-post`: every row neither step 1 nor step 2 claimed becomes x — 985 rows, all POST — and this is the NAMED LEFTOVER PILE the user's plan asked for, emitted with `source: floor` rather than as a silent x. (3) The wiring, which is what actually fixed the problem: step 2's wordless method floor became a DEFAULT that passes through to step 3, where before `poc/step2/flow.mjs` returned it and the x line never ran, so the 66 floor leaks were unreachable by construction. Step 2's own WORD claims (`modify-verb`, `modify-verb-summary`) stay final and step 3 never overrides them, because a word beats no word: precedence runs on evidence strength, not step order. Step 3 only ever emits x; it never loosens to r or w, and only step 1 assigns r. Step 3 ledger over the 4171-row corpus: `raise-word` 19 claimed / 19 right / 0 false alarms, across six providers (openai, zoom, meta-whatsapp, jira, asana, datadog); `floor-post` 985 claimed / 799 right (81.1%), truth r 33 / w 153 / x 799; total 1004 claimed / 818 right (81.5%) / **0 leaks** / 186 over-tight. The zero is structural, not luck: x is the tightest class, so step 3 has nothing looser to be wrong toward and its only possible error is over-tightness, a usability cost. It holds 186 of the flow's 186 over-tight rows, every one, all in `floor-post`. Whole flow, all three steps, n=4171: exact 3930 (94.2%), leaks 55 (1.3%), over-tight 186 (4.5%), against the pre-step-3 93.8 / 1.8 (74 rows) / 4.5 — 19 leaks closed for zero new over-tight rows. Emitted-class precision: r 2052 (2049 right, 3 loose-wrong), w 1115 (1063, 52 — was 71), x 1004 (818, 0). Step 2's method floor drops from 902 claims to 883. By source: `list` 343 rows / 337 right / 6 leaks / 0 over-tight; `floor` 3828 / 3593 / 49 / 186 (D77). The user accepts the fitted-versus-honest gap, extending D49's flagged-leak acceptance to step 3: LOVO with the list rebuilt per holdout closes 6 of the 66 at 0 false alarms and all 6 are the single word `permission`, because the other 16 words each appear in only one provider — so 19 is the fitted number and 6 the honest expectation on an unseen vendor, at zero usability cost in both readings. Measured and REJECTED on the way, do not re-try without new evidence: a standalone step 3 reading every row fresh with a POST x floor plus tightest-wins, which would claim all 1309 POST rows and drop the flow to 39.3% right by overriding step 1's 124 read-POSTs and step 2's 227 truth-w POSTs; adding PATCH to an x floor as the user's plan originally had it, 82 over-tight rows for 2 caught, the old corpus's 31% PATCH-x figure being 13 rows of only 42 against the new corpus's 2 of 84 — and moot anyway, since the pass-through ladder reaches PATCH on evidence; relationship nouns as the scan list (22 closed / 11 false alarms fitted, 12 / 9 LOVO, ratio 1.3 against the adoption bar of 10); un-verbs read anywhere (12 / 8) and at the lead token (7 / 9); the structural "remove X from Y" preposition read off the summary (10 / 28); and mining the DESCRIPTION text, the reserve card the project's own notes named for exactly this case, which failed in all 36 swept configurations — best fitted 34 / 22, the same configuration LOVO 10 closed / 37 false alarms, a ratio of 0.27, and forcing a word to fire for two providers gives 0 closures under LOVO. The honest limits stand: there is NO CLEAN EXAM — all three steps were measured on the same 4171 rows they were tuned on, which under D24 makes this corpus a tuning set; 47 leaks remain, every one on step 2's remaining 883-row wordless floor, and words have been measured out of reaching them; 153 truth-w POST rows never get off step 3's x floor; and 33 truth-r POST rows sit in step 3's pile as step 1's known misses, not step 3's to fix, since anything to r is the loosening direction and reaching them means unfreezing step 1 (D74). Decided 2026-09-17 at the user's word. Commits ad21062, 210464a, a145598. |
| D79 | The classifier GRADUATED from `poc/` to `src/` on 2026-09-17, in three checkpointed passes (tokeniser + step 1, step 2, step 3 + flow). The freeze is UNAFFECTED: D74, D75 and D78 freeze the rules and word lists, not the file locations, and `src/` reproduces them with 0 class / 0 rule / 0 source / 0 matched differences over all 4171 corpus rows with every word list pinned set-equal (`READ_VERBS` 23, `SAFE_VERBS` 10, `MODIFY_VERBS` 24, `OTHER_PARTY` 21, `RAISE_WORDS` 17), so graduation re-housed the frozen core rather than thawing it. `poc/` is KEPT, byte-untouched, as the frozen reference the src code is proved against, and `tools/proof-step1.js`, `tools/proof-step2.js` and `tools/proof-step3.js` are the equivalence proofs that compare the two continuously, row by row, on class, rule, source and matched. The shape change that made the rewrite worth doing: each step now returns `source` and `matched` ITSELF, at the moment a rule matches, so `sourceForRule` and `matchedWordsForRule` — and D77's six pins that existed only to guard that re-derivation against drift — are DELETED rather than ported; the POC's helper asked "did anything match?" and so had to re-implement every rule a second time to recover which word, while asking "which members matched?" once leaves nothing to re-derive. Four defects in the frozen POC were exposed by rebuilding against the rule instead of copying, none of which changed a published number: `FLOOR_RULES` exported but imported nowhere; two writers for which raise word hit (`applyStep3`'s `.some()` vs `matchedWordsForRule`'s `.filter()`); a latent bug in that duplicate, where `matchedWordsForRule` ignores an injected LOVO word list and always reads module-level `RAISE_WORDS` (with a rebuilt list of only `memberships` it reports `membership+memberships`, a word the list does not contain — unreachable in the POC, so never wrong in print); and two writers for the step-3 verdict, `flow.mjs` hand-building the `floor-post` literal while `step3.mjs` owned `raise-word` (now `src/step3.js` exports `floorPost()`). The package entry point was fixed, and it was two bugs not one: `exports` pointed at a nonexistent `./src/index.js`, and `files` was ["README.md","CHANGELOG.md","LICENSE"], so even a valid entry point would have shipped no code and 404'd for every adopter; the tarball went from 4 files with no code to 23 files, 25.2 kB packed, carrying `src/` and `types/`. The public surface is exactly ONE export, `classifyRow`, from `src/index.js` — the steps, word lists, tokeniser, `wordsForStep3` and `floorPost` are internals sealed by the exports map, not merely undocumented — and the D76/D77 published-map emitter is deliberately still not built, being its own pass after step 3. Numbers unchanged: 94.2% exact / 1.3% leaks / 4.5% over-tight on 4171 rows. Verification: 153 tests pass, typecheck clean, all five src proofs and all three poc proofs print "All pins hold."; orchestrator-verified by independent re-run of every check and by a deliberate break of the flow's precedence branch that printed 16 mismatches, including the pre-step-3 93.8% exact / 1.8% leaks, before the file was restored byte-identical. Decided 2026-09-17 at the user's word. Commit: the commit carrying this row. |
| D81 | Step 2 is CLOSED as of 2026-09-19. The D75 shape stands as THE step 2, not a waypoint: no further word-list mining, carving or tuning on step 2. Evidence: the D75 flow scored 85.3% exact on the clean exam, blind and once (okta, docusign, xero, 1383 rows), and nothing measured since beats it — a mined PUT/DELETE/PATCH list collapsed from 8.17 fitted to 0.83 leave-one-vendor-out, and a per-document profile steer lost to the bare method floor under LOVO (77.5% exact against 80.5%). On the 1817-row build set, 144 of step 2's 147 leaks sit on its wordless method floor; its word rules leak 3 of 22 claims. The same noun carries opposite meanings at different vendors, which vocabulary cannot read. The deterministic tool's stated scope is therefore roughly 80-90% of the problem; the last mile is D82. Lifting this needs the user's explicit word. |
| D82 | The last mile is PARKED for an optional model tier: TypeSafe Jev (https://flaviocopes.com/jev/), a calibrated decision model that answers typed questions with probabilities, with an implementation pattern in https://github.com/devagrawal09/jev-review. Not built: no API access yet (waiting list); the user will say when a key exists. Shape agreed in principle, for when it is built: (1) the deterministic D75 flow decides every row it has evidence for, unchanged; (2) the model is asked only about rows the flow cannot settle — step 2's wordless method-floor rows; (3) it may only RAISE (w to x), never lower, so a wrong answer costs over-tightness and never a leak; (4) it is OPTIONAL — the core works fully without it; (5) every answer records the model version, because the map is signed by the Resource Owner and a silent model update must not change a signed map; (6) opt-in discloses that the adopter's spec text is sent to an outside service. Adopting it reverses the PRD's "no model/LLM tier" out-of-scope line and the "no vendor lock-in" rule for this tier only, which is why it needs its own decision. First measurement when a key exists: one question on the build set's 1091 floor rows, raise-only, priced at the standing bar of 10 leaks closed per over-tight added, then scored once on a fresh exam from the ten locked vendors. Accuracy is capped by label truth itself — labellers agreed with calibration truth 85.3% — so a result reads against that ceiling, never against 100%. |
| D83 | M1 CLOSES as the mechanical offering, 2026-09-19, and the D30 go/no-go gate is AMENDED to match (user ruling). What M1 is: the deterministic D75 flow in src/, unchanged — spec text in, one class per operation out, no model, no network. What the D30 gate now means: condition 1 (zero leaks among assigned rows) is measured on the mechanical tool PLUS the opt-in Jev tier together, not on the mechanical tool alone, because the rows it fails on are exactly the rows Jev is placed to review; and "sent to review" (D30's own term for floor rows on PUT/DELETE/PATCH) now means "sent to Jev". Conditions 2-5 are unchanged. The mechanical tool's measured standing, stated rather than gated: on the 15-provider tuning corpus, GET rows (47.0%) are 99.9% right and rows where a word fired (8.2%) are 98.3% right, together about 55% of an API known on evidence; the PUT/DELETE/PATCH wordless floor (21.2% of rows, 94.7% right) holds 47 of the 55 leaks; the POST x floor (23.6%, 81.1% right) is over-tight only, never a leak. On the clean exam, blind: 85.3% exact, 12.4% leaks, 2.3% over-tight, with the PUT/DELETE/PATCH floor at 32.2% of rows and 151 of 171 leaks. D82 is WIDENED: Jev is sent every row step 2 labels w — its wordless floor rows AND its word claims — because the gate's 20 failing exam rows (9 xero updateOrCreate, 11 okta lifecycle) are word claims, not floor rows. Jev still only raises (w to x), never lowers, and is never sent GET r rows or POST x rows: the first need nothing, the second are already the tightest class. Sending all of step 2's w rows rather than only the wrong ones is unavoidable — knowing which are wrong is the problem Jev solves — and costs roughly $0.02 per thousand rows at TypeSafe's published price. The promise to adopters once Jev is available: near-complete answers at a fraction of a model's usual cost, stated as an aim and read against the truth ceiling (85.3% labeller agreement on calibration), never as a measured 100%. |
| D84 | The core is PARKED at v0.3.0, 2026-09-21 (user ruling): the D75/D78 flow in src/ stays frozen, word lists are kept and frozen (no additions, no removals), no more mining or model tuning. The next shape is tried as a POC (`poc/unreviewed/`) while it stays parked: w only on word evidence; every write row with no word is published as x with `evidence: floor` — the review queue — which is the tighter class on doubt, so no new class value and no carrier change. A guard treats queue rows as x until the Resource Owner marks them in the declared menu. Jev is demoted from D82/D83's raise tier: it never decides a class and at most orders the queue, likely-x first. Measured on the combined 6557-row diagnostic set (tuning data, not an exam): today's flow leaks 308 (4.7%) with 298 over-tight (4.5%); the D84 shape leaks 38 (0.6%) with 1996 over-tight (30.4%); the queue is step 2's 1969 wordless floor rows, 270 of them truth x. Why: every flow leak but 3 is step 2's, 270 of 305 on its wordless floor, where specs do not state the answer — mined lists collapse under LOVO (D81) and Jev closes 86 of 305 for 9 false alarms under LOVO (ratio 9.56, under the bar of 10). D83's amended gate stands until the POC is scored: the POC's own gate is leaks under 1% of rows on a fresh exam from the unused locked vendors (dropbox, shopify, linear), over-tight reported beside it. Detail: PRD section "Next shape: say unreviewed instead of guessing (D84, POC)". |
| D85 | D84 is REJECTED, 2026-09-22 (user ruling), before its POC was built; `poc/unreviewed/` is never created. Why: D84 moves step 2's PUT/DELETE/PATCH wordless floor from w to x. On the combined 6557-row set that trades 270 fewer leaks for 1698 more over-tight rows — 0.16 leaks closed per false alarm against the project's standing adoption bar of 10 leaks closed per false alarm, about 60x under it. It is not a better classifier; it is the same wordless guess relabelled, and it fails the project's own bar. The honesty D84 wanted already exists: `evidence: floor` is published on every verdict (D76/D77), so the safety decision belongs in the adopter's runtime policy, not in the class. What stands instead: the class stays the tool's best guess (a floor PUT says w and is right 86% of the time on the combined set, 1698 of 1969), `evidence` and `destructive` are published as they already are, and the README and PRD state a recommended consumption policy — r: allow; w with evidence list: allow; w with evidence floor: ask once, then remember the answer for that operation; x: ask every time; destructive true: ask every time, whatever the class. The policy is not carried in the map JSON; the map carries only class, destructive and evidence (D76/D77). Vendor-to-vendor inconsistency in how methods are used is structural (D81) and is reported through the floor flag, not chased; the tool cannot know which operations an adopter calls heavily, so it gives the head start and the adopter tightens from traffic. Jev returns to D82/D83's role — an optional raise-only tier (w to x, never lower), measured at 31 of 82 leaks closed for 0 false alarms on the 2026-09-20 exam and 86 of 305 for 9 under LOVO on the combined set (ratio 9.56, just under the bar of 10) — pending against the bar, not adopted. Nothing in src/ changes. Detail: PRD section "How to consume the map: the runtime policy (D85)". |
| D86 | One shared meaning of r/w/x, ADOPTED 2026-09-22 (user ruling), agreed in principle with the bareguard session the same day (bareguard: the user's agent gate — one hand-written file of r/w/x letters per tool or command, agent grants like `r--`/`rw-`/`rwx`, child ≤ parent, not-in-file = denied, no runtime asks). The definition: r = changes nothing; w = changes only the caller's OWN stuff, AND can be undone, AND is safe to repeat; x = any change that fails one of those — reaches another party, OR can't be undone, OR isn't safe to repeat; unsure → x. This is D20's x-definition plus one more disjunct, "can't be undone", folded INTO the class. It is strictly tighter than today: rows move w→x and never the other way, so the one invariant holds. D28's two-axes ruling is SUPERSEDED: `destructive` stops being an orthogonal axis — `destructive: true` ⇒ class x, always — and is KEPT as a refinement flag inside x (x+destructive vs x non-destructive) so MCP `destructiveHint` can still be fed; `evidence` (list/floor) is unchanged. D28's motivating case, "read and reply, never delete", is expressed in bareguard as a grant of `r-x` plus a deny flag on the delete action, so nothing is lost. Option chosen: 1 — the fold with a PROXY for "can't be undone", now: DELETE method, or lead verb in the 23-word destructive list (`poc/archive/v2/m0/destructive.json`: delete remove purge wipe destroy erase clear drop revoke unassign uninstall detach terminate cancel kill reset truncate expire disable deactivate suspend ban block); no relabel now. Option 2 — relabel the 6557 rows for reversibility, nine labellers — is deferred to BEFORE the next fresh exam, not before shipping. Measured on `data/combined-2026-09-21/` (6557 rows, 23 providers, tuning data), proxy applied identically to truth and prediction: truth w rows that become x 888 of 2266 (39%); truth becomes r 2705 / w 1378 / x 2474 (w falls from 35% to 21% of rows); tool w rows that become x 999 of 2335; ledger today exact 5951 (90.8%) / leaks 308 (4.7%) / over-tight 298 (4.5%), folded under the proxy exact 6088 (92.8%) / leaks 184 (2.8%) / over-tight 285 (4.3%). CAVEAT, stated wherever the number is: reversibility was never labelled; the proxy flips truth and prediction with the same rule, so where it applies it cannot disagree with itself; these are NOT accuracy numbers and are never quoted as an exam; the honest number needs the relabel. Effects: the wordless floor (today 1969 wordless PUT/DELETE/PATCH rows holding 270 of 308 leaks) loses DELETE — the method is now evidence for x — so the floor becomes wordless PUT/PATCH, roughly 1000 rows. Step 2 must be REBUILT: DELETE floors at x, the destructive verb list becomes a raise (w→x), the PUT/PATCH floor stays w. That is new `src/` code, a new module (M3), not a patch; the burned exams cannot re-score it (D24). The next fresh exam draws on dropbox, shopify, linear after the exposure check, after the relabel. Jev (D82/D83) keeps its role — optional, raise-only (w→x), never lowers, never sees x rows, never sees word-claim w rows (on the combined set: modify-verb rows 5 closed / 4 false, ratio 1.25; modify-verb-summary rows 4 / 2; the wordless floor 94 / 3, ratio 31 — it earns its place only where the tool read nothing); its pile shrinks with the floor; its criteria text changes from "reaches beyond the caller" to the three-clause definition, a criteria edit for M3, not a redesign. bareguard alignment (pending the user on both sides): bareguard owns the gate, the file format (one letter per row), agent grants, child ≤ parent, deny-by-absence, no runtime asks; rwxmap owns the labels and carriers, and gains one offline exporter (M3 item): spec → draft `tools` section of `bareguard.rwx.json` keyed by operationId, letter = class (identity under D86), NEVER the `agents` section; floor PUT/PATCH rows are LEFT OUT, so deny-by-absence forces a human letter and a missed row is a loud deny, never a leak; a sidecar review report lists every omitted row with class + evidence; `evidence` never enters the gate file; a human reviews the sidecar and commits the file; nothing writes at runtime. This REPLACES D85's runtime policy table: "ask once on w-floor / ask every time on x" becomes "review floor rows once before deploy; the gate never asks". Detail: PRD sections "The shared definition (D86)" and "How to consume the map (D85, revised by D86)". |
| D87 | The r/w/x definition collapses to the chmod reading; "touches others" stops being a class test. ADOPTED 2026-09-22 (user ruling). SUPERSEDES D86's definition and its three roads to x. The definition: r = read: changes nothing. w = write: changes things, the caller's own or anyone else's, in a way a later write can set back — sets, edits, creates, toggles, archives, pauses, cancels of something that can be resumed. x = execute: cannot be undone — deletes and removals, revokes, expires, voids, sends, publishes, charges, pays, refunds, triggers a run; `destructive: true` when it removes. Unsure → x. Why: every list that failed leave-one-vendor-out in this project was a noun list answering "whose thing is it" (user, member, role, permission, and the description mining behind them); nouns for "whose" mean different things per vendor (D81). Every list that held was a verb list at the lead token answering "what does the call do": step 1's read verbs hold at 98-100%, step 2's modify verbs at 88-94% per rule. D86 folded reversibility in but kept "reaches another party" as a road, so the wordless floor still carried the unreadable question and 270 of 308 leaks. D87 keeps only questions of the second kind. "Not safe to repeat" is folded into "cannot be undone" (what makes a repeat unsafe is that the first call already did something a write cannot take back); a plain create is a write (a delete undoes it, a duplicate is a mess not damage) and becomes w; a create that also sends, charges or runs stays x. What it costs: PUT/PATCH rows that reach another person (set a user's password, change their role, update collaborator permissions, publish/share by a non-create call) move from x to w. The tool never caught these (they were the floor leaks), so the tool is not less safe; truth now matches what a mechanical classifier can read. This is stated as a definition change with its own number, not as a fix, because it is the "relabel your way out" pattern D85 rejected for D84 and must be visible as such. MCP's openWorldHint is the slot "touches others" would map to; it stays out of the class and may return as an evidence-only flag next to `destructive`, never emitted as false; today there is no evidence source, so it is absent. Effects: the change is not one-directional (truth x can become w), so the relabel covers every non-r row of the combined set: 3852 of 6557 (2266 truth-w + 1586 truth-x), under a brief v3, calibrated blind (practice with two labellers, user rules disputes, holdback once). Brief v2, its practice run (100 of 100 agreed) and the 2266-row plan are discarded (kept in history at bad0c89). Step 2 under D87: DELETE → x by method; a can't-undo lead verb → x on any write method (list to be written by reading the pile and priced on the relabel: delete remove purge revoke expire void send publish trigger run execute charge pay refund…); PUT/PATCH otherwise → w by floor; POST → w on a modify verb or, if it passes the bar, a create verb, else x by floor. The POST floor stays x so an unknown create fails safe; the create-verb lowering ships only above the adoption bar of 10, and the definition does not depend on it. Every rule priced alone and jointly, LOVO over 23 providers. bareguard alignment and the exporter (D86) stand unchanged; letter = class. Jev's criteria text becomes the D87 definition. D86's proxy numbers (888 of 2266; the POC read 912) are retired to learnings with D86. Detail: PRD sections "The shared definition (D87)" and "Where the work is". |
| D88 | Jev may LOWER on the D87 POST floor (user ruling 2026-09-22), reversing D82 point 3 (raise-only) for the floor-post rows only. Everything else in D82 stands: optional, the core works without it, every answer records the model version, opt-in discloses that spec text goes to an outside service. The mechanical flow still decides every row it has a word or a method for; Jev is asked only about rows that reach the POST floor, cold, with criteria from BRIEF-v3. A row lowers to w only when p(x) is at or under a threshold chosen by leave-one-vendor-out against a ratio bar; the threshold and bar are PENDING the user's choice (measured: bar 25 -> t=0.20, 35 new leaks; bar 50 -> t=0.10, 8 new leaks; both identical on all 23 folds). Tuning data; the fresh exam decides adoption. |
